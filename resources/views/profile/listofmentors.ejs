<section class="flex justify-end mt-4 mr-8">
    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-5 rounded-full">
     <a onclick="document.getElementById('logout').submit()" href="/">Logout</a>
    </button>
  </section>

<section class="flex justify-center text-4xl mt-10"><h1>Mentor List DashBoard</h1></section>

<% for(var i=0; i<hi.length; i++) {%>
<section class="flex justify-center -mb-10 mt-10 ml-10">
<div class="max-w-sm rounded overflow-hidden shadow-lg">
  <div class="px-6 py-4">
    <h1><!-- <% const a = localStorage.getItem('name'); %>
      <% const b = JSON.parse(a); %>
      <% const c = (b.room); %> -->
    </h1>
    <div class="font-bold text-xl mb-2"> 
  <!-- <% if(hi[i].firstname == c ){ %>
  if part
  <% } else { %>
  else paet
  <% } %> -->
  <%= hi[i].firstname %>
  </div>
    <p class="text-gray-700 text-base">
      <%= hi[i].description %>
    </p>
  </div>
  <div class="px-6 pt-4 pb-2">
    <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"><img src="https://tailwindcomponents.com/svg/logo-color.svg"></span>
    <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"><%= hi[i].areaofexpertise %></span>
  </div>
  <h1><a href="/mentordetails?name=<%= hi[i].firstname %>&description=<%= hi[i].description %>&zip=<%= hi[i].zip %>">More</a></h1>
<!--   <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"><button>View More</button></span> -->

                <!-- <form action="/">


<input
                            type="text"
                            name="username"
                            id="usernameww"
                            placeholder="Enter username..."
                            value="a"
                            
                        />
 
                    <button type="submit" class="btn">More</button> -->

  </div>
  <% } %>
</div>



</section>